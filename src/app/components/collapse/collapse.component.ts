import {Component} from "@angular/core";

@Component({
    moduleId: module.id,
    selector: "collapse",
    templateUrl: "./collapse.component.html"
})

export default class Collapse {
    isCollapsed = false;
}